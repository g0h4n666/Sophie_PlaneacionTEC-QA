[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["chmod +x node_modules/.bin/* && node ./node_modules/vite/bin/vite.js build"]

[start]
cmd = "node server.js"
